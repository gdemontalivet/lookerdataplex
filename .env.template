# Dataplex Looker Integration Configuration
# Copy this file to .env and fill in your actual values

# Required: Your Google Cloud Project ID
GCP_PROJECT_ID=your-gcp-project-id

# Optional: GCP Location (defaults to "eu")
GCP_LOCATION=eu

# Optional: Dataplex Entry Group name (defaults to "looker")
DATAPLEX_ENTRY_GROUP=looker

# Optional: Your Looker instance ID (defaults to "mylooker")
LOOKER_INSTANCE_ID=your-looker-instance

# Optional: BigQuery dataset name (defaults to "retail_banking")
BQ_DATASET=retail_banking

# Optional: Enable debug mode (defaults to "false")
DEBUG=false

# Instructions:
# 1. Copy this file: cp .env.template .env
# 2. Edit .env with your actual values
# 3. Never commit .env to git (it's in .gitignore)
